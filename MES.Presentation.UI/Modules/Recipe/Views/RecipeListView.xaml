<UserControl x:Class="MES.Presentation.UI.Modules.Recipe.Views.RecipeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MES.Presentation.UI.Modules.Recipe.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid ItemsSource="{Binding Items}" 
                  SelectedItem="{Binding SelectedItem}"
                  Style="{StaticResource IndustrialDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="★ Recipe Name" Binding="{Binding RecipeName}" Width="150"/>
                <DataGridTextColumn Header="★ Recipe Code" Binding="{Binding RecipeCode}" Width="120"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="80"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedDate, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="140"/>
                <DataGridTextColumn Header="Modified Date" Binding="{Binding ModifiedDate, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="140"/>
                <DataGridCheckBoxColumn Header="Blocked" Binding="{Binding IsBlocked}" Width="70">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="IsHitTestVisible" Value="False"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>
                <DataGridTextColumn Header="Batch Weight" Binding="{Binding BatchWeight, StringFormat=\{0:F2\}}" Width="120"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
