<UserControl x:Class="MES.Presentation.UI.Modules.Recipe.Views.RecipesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MES.Presentation.UI.Modules.Recipe.Views" 
             xmlns:controls="clr-namespace:MES.Presentation.UI.Controls.ListHeaderBar"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource LightWhiteBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:ListHeaderBarView Grid.Row="0" DataContext="{Binding Header}"/>

        <ContentControl Grid.Row="1" 
                        Content="{Binding CurrentContentViewModel}" 
                        Margin="10"/>
    </Grid>
</UserControl>
