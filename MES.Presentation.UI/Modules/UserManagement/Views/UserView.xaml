<UserControl x:Class="MES.Presentation.UI.Modules.UserManagement.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:common="clr-namespace:MES.Presentation.UI.Controls.ListHeaderBar"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <common:ListHeaderBarView
            Grid.Row="0"
            DataContext="{Binding Header}" />

        <Border Grid.Row="1"
                Background="White"
                Margin="8"
                CornerRadius="4">

            <ContentControl
                Content="{Binding CurrentContentViewModel}"
                Margin="8"/>
        </Border>

    </Grid>

</UserControl>
