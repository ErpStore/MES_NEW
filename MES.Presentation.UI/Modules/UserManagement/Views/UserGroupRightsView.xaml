<Window x:Class="MES.Presentation.UI.Modules.UserManagement.Views.UserGroupRightsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="500" Width="360"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="#EFEFEF" BorderBrush="#001C3B" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="45"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="#001C3B">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding WindowTitle}"
                           Foreground="White"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"
                           FontSize="13"
                           FontWeight="SemiBold"/>
                <Button Grid.Column="1"
                        Command="{Binding CancelCommand}"
                        Content="X"
                        Foreground="White"
                        Background="Transparent"
                        BorderThickness="0"
                        Width="30"
                        FontWeight="Bold"
                        Cursor="Hand"/>
            </Grid>

            <!-- Search Box -->
            <Grid Grid.Row="1" Margin="8,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Style="{StaticResource SearchTextBox}"
                         Height="28"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="1"
                        Width="28" Height="28"
                        Margin="4,0,0,0"
                        Background="#001C3B"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        ToolTip="Refresh">
                    <TextBlock Text="↺" FontSize="14" FontWeight="Bold"/>
                </Button>
            </Grid>

            <!-- Column Headers -->
            <Grid Grid.Row="2" Background="#001C3B" Margin="8,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Assignable authorization"
                           Foreground="White"
                           FontWeight="SemiBold"
                           FontSize="11"
                           Margin="8,0,0,0"
                           VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1"
                           Text="Assigned"
                           Foreground="White"
                           FontWeight="SemiBold"
                           FontSize="11"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Grid>

            <!-- Rights Tree -->
            <ScrollViewer Grid.Row="3" Margin="8,2,8,0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Background="White"
                          BorderBrush="#DDDDDD"
                          BorderThickness="1">
                <TreeView ItemsSource="{Binding RightNodes}"
                          Background="Transparent"
                          BorderThickness="0">
                    <TreeView.Resources>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="True"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="2,2"/>
                        </Style>
                    </TreeView.Resources>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <Grid Margin="0,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding DisplayName}"
                                           FontSize="11"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>
                                <CheckBox Grid.Column="1"
                                          IsChecked="{Binding IsAssigned, Mode=TwoWay}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Visibility="{Binding IsLeaf, Converter={StaticResource BoolToVisibility}}"/>
                            </Grid>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </ScrollViewer>

            <!-- Footer Buttons -->
            <Grid Grid.Row="4" Margin="8,0,8,8">
                <Button Content="Accept"
                        Command="{Binding AcceptCommand}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Width="80" Height="28"
                        Background="#001C3B"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border"><Setter Property="CornerRadius" Value="0"/></Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
