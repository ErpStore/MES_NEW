<UserControl x:Class="MES.Presentation.UI.Modules.UserManagement.Views.UsersListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Background="White">
    <Grid>
        <Grid>
            <DataGrid ItemsSource="{Binding Users}" 
                  SelectedItem="{Binding SelectedUser}"
                  Style="{StaticResource IndustrialDataGridStyle}">

                <DataGrid.InputBindings>
                    <MouseBinding MouseAction="LeftDoubleClick" 
                              Command="{Binding EditUserCommand}"/>
                </DataGrid.InputBindings>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="User Name" Binding="{Binding UserName}" Width="150"/>

                    <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120"/>

                    <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="120"/>

                    <DataGridTextColumn Header="Departments" Binding="{Binding Department}" Width="150"/>

                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>

                    <DataGridTextColumn Header="Mobile" Binding="{Binding Mobile}" Width="120"/>

                    <DataGridTextColumn Header="Password Expiry Date" 
                                    Binding="{Binding PasswordExpiryDate, StringFormat=dd-MM-yyyy}" 
                                    Width="150"/>

                    <DataGridTemplateColumn Header="Active" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive}" 
                      HorizontalAlignment="Center" 
                      VerticalAlignment="Center"
                      IsEnabled="False"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Actions" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">

                                    <Button Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                        CommandParameter="{Binding}"
                        ToolTip="Edit User"
                        Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                                        <TextBlock Text="✎" FontSize="16" Foreground="#001C3B" FontWeight="Bold"/>
                                    </Button>

                                    <Button Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                        CommandParameter="{Binding}"
                        ToolTip="Delete User"
                        Background="Transparent" BorderThickness="0" Cursor="Hand">
                                        <TextBlock Text="🗑" FontSize="16" Foreground="#D32F2F"/>
                                    </Button>

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
