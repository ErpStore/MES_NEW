<UserControl x:Class="MES.Presentation.UI.Modules.Order.Views.OrderManagementListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid ItemsSource="{Binding Items}" 
                  SelectedItem="{Binding SelectedItem}"
                  Style="{StaticResource IndustrialDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="S.No." Binding="{Binding SerialNumber, StringFormat=\{0:D2\}}" Width="60"/>
                <DataGridTextColumn Header="Recipe Name" Binding="{Binding RecipeName}" Width="150"/>

                <DataGridTextColumn Binding="{Binding RecipeCode}" Width="150">
                    <DataGridTextColumn.Header>
                        <TextBlock><Run Text="★" FontSize="10" BaselineAlignment="Top"/>Recipe Code</TextBlock>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>

                <DataGridTextColumn Binding="{Binding SetBatch}" Width="100">
                    <DataGridTextColumn.Header>
                        <TextBlock><Run Text="★" FontSize="10" BaselineAlignment="Top"/>Set Batch</TextBlock>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Actual Batch" Binding="{Binding ActualBatch}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>

                <DataGridCheckBoxColumn Header="Release" Binding="{Binding IsReleased}" Width="80">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="IsHitTestVisible" Value="False"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>

                <DataGridTextColumn Header="Batch Start" Binding="{Binding BatchStart, StringFormat=\{0:yyyy-MM-dd HH:mm:ss\}}" Width="160"/>
                <DataGridTextColumn Header="Batch End" Binding="{Binding BatchEnd, StringFormat=\{0:yyyy-MM-dd HH:mm:ss\}}" Width="160"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
