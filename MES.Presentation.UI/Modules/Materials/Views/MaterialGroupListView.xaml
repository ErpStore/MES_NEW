<UserControl x:Class="MES.Presentation.UI.Modules.Materials.Views.MaterialGroupListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MES.Presentation.UI.Modules.Materials.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  Style="{StaticResource IndustrialDataGridStyle}">

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding MaterialName}" Width="250">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="★ Material Group"/>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Description" 
                                    Binding="{Binding MaterialDescription}" 
                                    Width="*"/>

                <DataGridTemplateColumn Header="Actions" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">

                                <Button Command="{Binding DataContext.EditMaterialGroupCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        ToolTip="Edit MaterialGroup"
                                        Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                                    <TextBlock Text="✎" FontSize="16" Foreground="#001C3B" FontWeight="Bold"/>
                                </Button>

                                <Button Command="{Binding DataContext.DeleteMaterialGroup, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        ToolTip="Delete MaterialGroup"
                                        Background="Transparent" BorderThickness="0" Cursor="Hand">
                                    <TextBlock Text="🗑" FontSize="16" Foreground="#D32F2F"/>
                                </Button>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
