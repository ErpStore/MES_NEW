<UserControl x:Class="MES.Presentation.UI.Modules.Materials.Views.MaterialManagementListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="White">
        <DataGrid ItemsSource="{Binding Items}" 
                  SelectedItem="{Binding SelectedItem}"
                  Style="{StaticResource IndustrialDataGridStyle}">

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding MaterialCode}" Width="120">
                    <DataGridTextColumn.Header>
                        <TextBlock><Run Text="★" FontSize="10" BaselineAlignment="Top"/>Material Code</TextBlock>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Material Name" Binding="{Binding Name}" Width="150"/>

                <DataGridTextColumn Binding="{Binding MaterialGroupName}" Width="150">
                    <DataGridTextColumn.Header>
                        <TextBlock><Run Text="★" FontSize="10" BaselineAlignment="Top"/>Material Group</TextBlock>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Handling / Safety Info" Binding="{Binding HandlingInfo}" Width="160"/>

                <DataGridTextColumn Header="Density" Binding="{Binding Density, StringFormat=\{0:0.0\}}" Width="80"/>

                <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="120"/>

                <DataGridTextColumn Binding="{Binding Unit}" Width="80">
                    <DataGridTextColumn.Header>
                        <TextBlock><Run Text="★" FontSize="10" BaselineAlignment="Top"/>Unit</TextBlock>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Min Level" Binding="{Binding MinLevel, StringFormat=\{0:000.000\}}" Width="100"/>

                <DataGridTextColumn Header="Shelf Life Days" Binding="{Binding ShelfLifeDays}" Width="110"/>

                <DataGridCheckBoxColumn Header="Blocked" Binding="{Binding IsBlocked}" Width="80">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="IsHitTestVisible" Value="False"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>

                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
